# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# macOS
.DS_Store

# Experiment outputs — keep binary .pkl artifacts out of git (they are
# large and regeneratable via scripts/run_all_experiments.sh).
results/
# Figures are small (~1MB total) and committed so graders can view them
# without running the full campaign.
# figures/  -- intentionally NOT ignored

# Batch-run log files
.logs/

# Development notes and throwaway scripts (not needed for reproduction)
dev/

# LaTeX build artifacts
*.aux
*.log
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
.build/

# IDE
.vscode/
.idea/
